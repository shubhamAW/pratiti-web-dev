function add(x,y){
    return x+y;
}
function sub(x,y){
    return x-y;
}

module.exports={add , sub};

//Notes: 
//wrong file name if o> node hello.js (hello not exist )
// it says module not found not a file not found
// so your code is called as a module.

